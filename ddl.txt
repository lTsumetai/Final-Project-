url dataset: https://www.kaggle.com//kjashgd

-- DDL
CREATE TABLE IF NOT EXISTS students_performance (
    "gender" TEXT,
    "race/ethnicity" TEXT,
    "parental level of education" TEXT,
    "lunch" TEXT,
    "test preparation course" TEXT,
    "math score" INTEGER,
    "reading score" INTEGER,
    "writing score" INTEGER
);


-- DML
COPY students_performance
FROM 'C:\Users\Aufa\Downloads\StudentsPerformance.csv'
DELIMITER ','
CSV HEADER;

